version: "3.3"
services:
  viyaapp:
      build: .
      restart: always
      ports:
        - 5002:8080
      environment:
        - VIYA_SERVER=https:xxxx
        - APPNAME=ORAPP
# Mounts volume for tokens(if using saved tokens, and other files)
# volumes:
#    - c:/Public/dev/data/sas:/sasin